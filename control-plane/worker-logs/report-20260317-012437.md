# Shift Report — 2026-03-17T01:24:37Z

## Task: Debug leverage cap blocking realistic position opening

## Result: ROOT CAUSE IDENTIFIED - Critical decimal format mismatch

Executed systematic on-chain debugging to find why LeverageModel.getEffectiveMaxLeverage() returns 1.0x instead of expected 25-30x for SpaceX market.

**Key Findings:**
- SpaceX market should allow ~25-30x leverage based on time-to-resolution (expires Dec 2026)
- LeverageModel returns exactly 1.0x (floored at minimum)
- Platform ceiling = 3.0x instead of expected 30x
- TVL_Multiplier = 0.1x instead of expected 1.0x ← ROOT CAUSE

**Technical Analysis:**
- BASE_MAX = 30x ✅
- TVL_Mult = 0.1x ❌ (should be 1.0x)
- IFR_Mult = 1.0x ✅
- Util_Mult = 1.0x ✅
- Platform Ceiling = 30x × 0.1x × 1.0x × 1.0x = 3.0x

**Root Cause:** Decimal format mismatch between:
- LeverVault.totalAssets() returns 60504028315742 (USDT 6-decimal = 60.5M USDT)
- LeverageModel expects WAD 18-decimal format
- LeverageModel treats 60.5M USDT as 0.00006 WAD (essentially zero TVL)
- Formula: TVL_Mult = sqrt(0 / 50M) = 0 → clamped to TVL_MULT_FLOOR = 0.1x

**Expected:** sqrt(60.5M / 50M) = sqrt(1.21) = 1.1 → clamped to max 1.0x

## QA Gate:
- **Compile:** PASS (no code changes)
- **Tests:** Not applicable (investigation task)
- **Spec match:** Identified spec deviation - decimal format inconsistency
- **Regressions:** None

## Issues Found:
**CRITICAL** added to known-issues.md: Decimal format mismatch in LeverageModel TVL calculations blocking all realistic leverage trading. Single bug crushes platform ceiling by 10x and prevents 5x-30x position opening required for investor demo.

## Next Priority:
**P0** Fix the decimal mismatch bug - either modify LeverageModel to scale vault.totalAssets() by 1e12 or fix LeverVault to return WAD format. This is blocking ALL high-leverage trading, market maker operations, realistic OI generation, and proper fee revenue calculations.

## Build Health:
BLOCKED by leverage model bug preventing realistic trading operations. Investigation complete, fix implementation required before protocol can demonstrate proper leverage functionality.