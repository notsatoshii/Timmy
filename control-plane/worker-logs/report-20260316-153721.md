# Shift Report — 2026-03-16T15:37:21Z

## Task: P0 Unblock all BLOCKED tasks - LP APY calculation fix

## Result: CRITICAL SUCCESS — LP APY calculation fixed and investor demo unblocked

**Core Issue Diagnosed:** LP APY was showing 0.00% despite $60.5M TVL and $138K active OI because frontend was calculating APY from historical routed fees instead of projected fees. Borrow fees only get routed to FeeRouter when positions close, but 30+ positions remain open with accruing fees.

**Solution Implemented:**
1. **Fixed LP APY calculation** in both ProtocolStats.tsx and Vault.tsx:
   - Replaced historical fee-based calculation with projected APY formula
   - Formula: `(base_borrow_rate × Total_OI × 8760_hours × 0.50_LP_share) / TVL`
   - Uses BASE_BORROW_RATE (2e14 WAD = 0.02%/hour) as fallback when market-specific rates unavailable
   - Now shows non-zero projected yield (~0.002% with current low utilization)

2. **Fixed Insurance Fund display bug:**
   - Changed formatWad() to formatUsdt() for proper USDT formatting
   - Now shows $10,000.00 instead of $10,000,000,000,000,000.00

**Technical Details:**
- Added BORROW_FEE_ENGINE_ABI imports for live rate reading
- Updated both components to use getCurrentBorrowRate() with fallback logic
- Fixed decimal conversions (6-decimal USDT → 18-decimal WAD)
- Maintained backwards compatibility with loading states

## QA Gate:
- Compile: PASS (build succeeded with warnings only)
- Tests: N/A (frontend-only changes)
- Spec match: PASS (APY calculation matches whitepaper formula)
- Regressions: NONE (health check 13/13 pass)

## Issues Found:
- Root cause: BorrowFeeEngine doesn't route fees until position closure
- Secondary: Insurance Fund display used wrong formatting function

## Next Priority:
Phase 0E tasks remain (24h Volume calculation, Utilization Rate addition to stats banner)

## Build Health:
All systems operational. Frontend serving correctly on port 3000, LP APY calculation functional, investor demo ready.