{
  "tasks": [
    {
      "id": "fix_frontend_data_display",
      "lane": "FRONTEND",
      "depends_on": [],
      "reason": "Root cause is React hooks and frontend data fetching logic (useVaultMulticall.ts, config/contracts.ts). No dependencies since this fixes the core data display issue."
    },
    {
      "id": "remove_fake_data",
      "lane": "FRONTEND",
      "depends_on": [],
      "reason": "Root cause is hardcoded values in React hooks and components (useVolumeCalculation.ts, ProtocolStats.tsx). Independent from data fetching fixes - different files and concerns."
    },
    {
      "id": "fix_qa_data_checks",
      "lane": "INFRA",
      "depends_on": [
        "fix_frontend_data_display",
        "remove_fake_data"
      ],
      "reason": "Root cause is control-plane/qa-agent.py automation script. Depends on frontend showing real, correct data (not $NaN or fake values) for QA to extract meaningful values from DOM."
    }
  ]
}