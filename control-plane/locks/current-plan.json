{
  "tasks": [
    {
      "id": "2",
      "lane": "FRONTEND",
      "depends_on": [],
      "reason": "Root cause is React hook (useVaultMulticall) and RPC call issues from frontend. Requires fixing React components and data formatting."
    },
    {
      "id": "3",
      "lane": "FRONTEND",
      "depends_on": [
        "2"
      ],
      "reason": "React component display issue for positions tab. Same lane as task 2 and could touch shared hooks/components, so sequential for safety."
    }
  ]
}