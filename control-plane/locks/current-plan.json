{
  "tasks": [
    {
      "id": "2",
      "lane": "INFRA",
      "depends_on": [],
      "reason": "Root cause is web server configuration (nginx/Apache) to serve React app instead of directory listing. This is deployment infrastructure, not React code changes."
    },
    {
      "id": "3",
      "lane": "INFRA",
      "depends_on": [
        "2"
      ],
      "reason": "Updating health-check.sh and running verification scripts is infrastructure work. Depends on task 2 because cannot properly validate demo until frontend serves React app instead of directory listing."
    }
  ]
}